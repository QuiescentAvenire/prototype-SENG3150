package group2;

public class Search {
    //First, get what we have to work with, check all requirements we have applied
    //Check if there are words we are searching with
    //Check if that word lines up with a destination
    //From starting location, implement a pathfinding algorithm that will get to the destination from the starting one
    //Any additional things we need to search for (departure date etc.) can be added as filters as needed
    //Use the SQL sortby thing to order results
}
